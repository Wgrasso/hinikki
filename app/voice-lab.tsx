// app/voice-lab.tsx — hidden dev route for the Phase 1 voice spike (never linked from the UI;
// open via hinikki://voice-lab or by typing the URL in the dev client). Delete after Phase 3.
import VoiceLab from "../src/features/voice/VoiceLab";

export default VoiceLab;

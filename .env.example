# HiNikki environment — copy to .env and fill in (public anon key only, never the service-role key)
EXPO_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=YOUR-PUBLIC-ANON-KEY

# Voice needs NO client-side keys: the private ElevenLabs agent is reached through tokens minted
# by the elevenlabs-token Edge Function (secrets set via `supabase secrets set`, see elevenlabs/README.md).
